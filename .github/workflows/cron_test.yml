name: Twice Daily Cron Test

on:
  schedule:
    # 12:00 PM MYT (04:00 UTC)
    - cron: '0 4 * * *'
    # 4:00 PM MYT (08:00 UTC)
    - cron: '0 8 * * *'

jobs:
  cron-test:
    runs-on: ubuntu-latest

    steps:
      - name: Print timestamp
        run: echo "✅ Cron job triggered at $(date -u) UTC"
